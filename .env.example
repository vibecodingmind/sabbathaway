DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.0-flash"
GOOGLE_MAPS_PLATFORM_KEY=""
APP_URL="http://localhost:3000"
PORT=3000

# Stripe (optional — enables real Checkout; without it payments are simulated)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Email (optional — Resend preferred, or SMTP; without them emails are logged only)
RESEND_API_KEY=""
EMAIL_FROM="AdventistStay <noreply@adventiststay.org>"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_SECURE="false"
SMTP_FROM=""
